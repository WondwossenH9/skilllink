# Environment Configuration
NODE_ENV=development
PORT=3001

# JWT Configuration (REQUIRED - must be set to a secure random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# CORS Configuration  
FRONTEND_URL=http://localhost:3000

# Database Configuration for Development (SQLite)
DB_DIALECT=sqlite
DB_STORAGE=./dev.sqlite3

# Database Configuration for Production (PostgreSQL)
# Uncomment and configure these for production:
# NODE_ENV=production
# DATABASE_URL=postgresql://username:password@host:port/database
# DB_SSL=require

# Rate Limiting (Production)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Security Headers (automatically enabled in production)
# No additional configuration needed for helmet middleware
